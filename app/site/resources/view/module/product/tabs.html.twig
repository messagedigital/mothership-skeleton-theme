<section class="tabs row">
	{# Details #}
	<div class="details">
		<h3>Details</h3>
		{% if content.details.value %}
			{{ content.details }}
		{% else %}
			<p>{{ product.description | nl2br }}</p>
		{% endif %}
	</div>

	{# Fitting guide #}
	{% if product.details.sizing.value %}
		<div class="fitting">
			<h3>Fitting guide</h3>
			{{ product.details.sizing }}
		</div>
	{% endif %}

	{# Shipping and returns #}
	{% if content.shipping.value %}
		<div class="shipping">
			<h3>Shipping &amp; returns</h3>
			{{ content.shipping }}
		</div>
	{% endif %}

</section>