{# Flash messages #}
{% for type, messages in app.session.getFlashBag.all %}
	<div class="feedback {{ type }}">
		{% for message in messages %}
			<p>{{ message|raw }}</p>
		{% endfor %}
	</div>
{% endfor %}